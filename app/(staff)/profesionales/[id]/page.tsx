// TODO: Fase 5 — Detalle de profesional: horarios, especialidad, bloqueos
export default function ProfesionalDetallePage({ params }: { params: { id: string } }) {
  return <div>Profesional {params.id}</div>
}
